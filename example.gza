-- Ghostlang example for testing LSP
local greeting = "Hello from Ghostlang!"
local version = "0.1.0"

function greet(name)
    return greeting .. " " .. name
end

function main()
    local result = greet("Grim Editor")
    print(result)
    print("Version: " .. version)
end
